<%= form_for([@podcast, @podcast_review]) do |f| %>
  <% if @podcast_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@podcast_review.errors.count, "error") %> prohibited this podcast_review from being saved:</h2>

      <ul>
      <% @podcast_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "field podtitle" %>
  </div><br>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, :class => "field podcontent", :placeholder => "Schizzle my nizzle" %>
  </div><br>
  <div class="actions">
    <%= f.submit " Submit ", :class => "btn btn-primary" %>
  </div>
<% end %>
