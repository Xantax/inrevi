<% provide(:title, 'Paintings, Photography, Street-art...') %>
<hr>
          <%= form_tag finearts_search_path, :method => :get do %>
              <div class="searchbox mobile">
                <%= text_field_tag :search, params[:search], :placeholder => "e.g. Mona Lisa" , :class => "form-control" %>
              </div>
              <div class="searchgo">
                <span class="input-group-btn">
                  <%= submit_tag "Go", class: "btn btn-default" %>
                </span>
              </div>
          <% end %>
<hr>