<div class="row mobilehead">
  
  <div class="col-xs-3 headicon">
    <% if signed_in? %>
      <%= link_to image_tag('othericons/rss35.png'), mobile_newsfeed_path %>
    <% end %>
  </div>
    
  <div class="col-xs-6 mobilelogo">
    <%= link_to image_tag("logou8m.PNG", alt: "Inrevi - Power To The People"), root_path %>
  </div>
  
  <div class="col-xs-3 headicon">
    <% if signed_in? %>
      <%= link_to image_tag('othericons/profile7.png'), user_path(current_user) %>
    <% end %>
  </div>
  
</div>
  
  <% unless signed_in? %>
    <div class="row mobilecatheader">
        <div class="slogan-mobile">Power To The People</div>
    </div>
  <% end %>
