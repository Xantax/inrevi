<%= form_for :local_review, url: local_local_reviews_path do |f| %>

<%= f.hidden_field :local_id, :value => @local['factual_id'] %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "field podtitle" %>
  </p>

  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content, :class => "field podcontent" %>
  </p>

  <%= f.submit " Submit ", :class => "btn btn-primary" %>

<% end %>
