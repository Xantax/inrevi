<%= form_for([@auto, @auto_review]) do |f| %>
  <% if @auto_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_review.errors.count, "error") %> prohibited this auto_review from being saved:</h2>

      <ul>
      <% @auto_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "field podtitle" %>
  </div><br>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, :class => "field podcontent", :placeholder => "Tell the world what you like, dislike, favorite features..." %>
  </div><br>
  <div class="actions">
    <%= f.submit " Submit ", :class => "btn btn-primary" %>
  </div>
<% end %>