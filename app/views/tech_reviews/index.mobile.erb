<% if @avg_score == 0.0 %>
  <div class="noreviewsonindex">No reviews :( </div>
<% else %>
<% @tech_reviews.each do |tech_review| %><br>
      <% if tech_review.user.provider == "facebook" %>
          <div class="mobile-activity-photo">
            <%= image_tag(tech_review.user.image_auth, width: "50") %>
          </div> 
            <div class="reviewname mobile">     
              <%= link_to user_path(tech_review.user) do %> 
                <%= tech_review.user.first_name %>
                <%= tech_review.user.last_name.truncate(2, omission: '.')%>
              <% end %>
            </div>

      <% elsif tech_review.user.provider == "google_oauth2" %>
          <div class="mobile-activity-photo">
            <%= image_tag(tech_review.user.image_auth, width: "50") %>
          </div> 
            <div class="reviewname mobile">     
              <%= link_to user_path(tech_review.user) do %> 
                <%= tech_review.user.first_name %>
                <%= tech_review.user.last_name.truncate(2, omission: '.')%>
              <% end %>
            </div>

      <% elsif tech_review.user.provider == "email" %>
          <div class="mobile-activity-photo">
            <%= image_tag(tech_review.user.image, width: "50") %>
          </div> 
            <div class="reviewname mobile">     
              <%= link_to user_path(tech_review.user) do %> 
                <%= tech_review.user.name %>
              <% end %>
            </div> 

      <% elsif tech_review.user.provider == "twitter" %>
          <div class="mobile-activity-photo">
            <%= image_tag(tech_review.user.image_auth, width: "50") %>
          </div> 
            <div class="reviewname mobile">     
              <%= link_to user_path(tech_review.user) do %> 
                <%= tech_review.user.name %>
              <% end %>
            </div>

      <% end %>


       <br>
         <div class="separatereview">
           
            <div class="numberOne">
              <%= tech_review.point %>
            </div><br><br>
           
          <div class="reviewcontent"><%= simple_format(tech_review.content) %></div>
           
                <% tech_review.review_images.each do |pic| %>
                     <%= link_to image_tag(pic.image_url(:thumb), width: 50), pic.image_url, target: :_blank if pic.image?  %>
                <% end %>
                 <% if signed_in? %>
                   <% unless current_user == tech_review.user %>
                     <hr>
                   <% end %>
                 <% end %>
           
              <div class="bottomreviewicons">
                <% if signed_in? %>
                
                  <% if current_user == tech_review.user %>
                    
                  <% elsif current_user.voted_for? tech_review %>
                    Voted!
                  <% else %>
                   <div class="thumbsup">
                     <%= link_to "",  like_tech_tech_review_path(tech_review.tech_id, tech_review), method: :put, :remote => true %>
                   </div>
                            
                   <div class="thumbsdown">
                    <%= link_to "", dislike_tech_tech_review_path(tech_review.tech_id, tech_review), method: :put, :remote => true %>
                   </div>
                
                  <% end %>                
                
                <% end %>
                
              </div>
          
      </div><br>

  <% end %>
<% end %>

<div class="digg_pagination">
  <%= will_paginate @tech_reviews %>
</div>