<h1>Listing podcast_categories</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @podcast_categories.each do |podcast_category| %>
      <tr>
        <td><%= podcast_category.name %></td>
        <td><%= link_to 'Show', podcast_category %></td>
        <td><%= link_to 'Edit', edit_podcast_category_path(podcast_category) %></td>
        <td><%= link_to 'Destroy', podcast_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Podcast category', new_podcast_category_path %>
