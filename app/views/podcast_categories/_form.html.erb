<%= form_for(@podcast_category) do |f| %>
  <% if @podcast_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@podcast_category.errors.count, "error") %> prohibited this podcast_category from being saved:</h2>

      <ul>
      <% @podcast_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
