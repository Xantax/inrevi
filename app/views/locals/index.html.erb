<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>

<% provide(:title, 'Hotels, Bars, Doctors...') %>
<div class="row">

<div class="col-sm-2 categories">
  <%= render 'shared/categories' %>
</div>

<div class="col-sm-10">
  <div class="allnews">
    
<% model_class = Local %>
<div class="search-box">
  <%= render 'search_form' %>
</div>
    <div class="col-sm-6 local">
    <% @results.first(2).each do |result| %><br><br>
<div class="localindexname"><%= link_to result['name'], local_path(result['factual_id']) %></div>
<div class="localcategory"><b>·</b> <%= result['category_labels'].join(', ') unless result['category_labels'].blank? %></div><br>
          <%= result['address'] %><br>
          <%= result['locality'] %>, 
          <%= result['region'] %>, 
          <%= result['postcode'] %><br>
          <%= result['tel'] %>
      <script type="text/javascript">
      var markerLatLong = new google.maps.LatLng(<%= result["latitude"] %>, <%= result["longitude"]%>);
        var marker = new google.maps.Marker({
          position: markerLatLong,
          map: map,
          title: '<%= result["name"] %>'
        });  </script>
      
      <div class="localbottom"></div>
<% end %>
    </div>
    
    <div class="col-sm-6 localmap">
      <br><br>
      <div style='width: 300px;'>
        <div id="map" style='width: 300px; height: 300px;'></div>
      </div>
      
      <script type="text/javascript">
                handler = Gmaps.build('Google');
          handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
            markers = handler.addMarkers(<%=raw @hash.to_json %>);
            handler.bounds.extendWith(markers);
            handler.fitMapToBounds();
          });
      </script> 
      
    </div> 

  </div> 
</div> 

</div>
