<% provide(:title, @resource_type.to_s.pluralize.humanize) %>

  <%= render 'search_form' %>

  <% if @products %>
     <%= render 'products' %>
  <% end %>

  <% if @products %>
      <%= render 'pagination' %>
  <% end %> 

  <%= render 'subcategories' %>