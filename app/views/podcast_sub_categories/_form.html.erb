<%= form_for(@podcast_sub_category) do |f| %>
  <% if @podcast_sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@podcast_sub_category.errors.count, "error") %> prohibited this podcast_sub_category from being saved:</h2>

      <ul>
      <% @podcast_sub_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.collection_select :podcastcategory_id, @podcastcategories, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
