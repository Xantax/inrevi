 <%= render 'follow_form' if signed_in? %>
<% current_user ||= @user %>
   <strong><%= link_to current_user.name, user_path(current_user) %></strong><br>
  <%= image_tag(current_user.smallimage)%><div class="full_profile"><%= link_to "full profile", user_path(current_user) %> <br >
  <%= link_to "logout", signout_path, class: "sign_out" %></div> 
  <%= render 'shared/stats' %>
