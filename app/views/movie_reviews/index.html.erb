<h1>Listing movie_reviews</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @movie.movie_reviews.each do |movie_review| %>
      <tr>
        <td><%= link_to 'Show', movie_review %></td>
        <td><%= link_to 'Edit', edit_movie_movie_review_path(movie_review) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Movie review', new_movie_movie_review_path %>
