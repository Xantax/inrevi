<%= form_for([@movie, @movie_reviews]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

