<%= render "tvshowshow" %><br>

    <% if @promotion.present? && @promotion.tvshow %>
        <div class="row showpromotion">
          <a href="<%= @promotion.link %>" target="_blank"><%= @promotion.text %></a> 
        </div>
    <% end %>

  <div class="row"> 
    <%= render :file => "tvshow_reviews/index" %>
  </div>